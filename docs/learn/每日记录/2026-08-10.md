# 2026-08-10 学习记录

## 今日目标

- 理解 `Server` 对象从创建到停止的完整生命周期。
- 掌握默认方法注册与 `OverrideCallback()` 覆盖机制。
- 理解同步读取、异步通知和停止流程之间的关系。
- 继续用源码区分协议约定、Transport 约定和具体实现行为。

## 今日已完成

- [x] 确认阶段二的当前进度和学习范围
- [x] 定位 `Server` 构造、连接、停止及回调覆盖的源码入口
- [ ] 完成 Server 生命周期学习
- [ ] 完成回调覆盖机制学习
- [ ] 完成通知队列与并发停止流程学习
- [ ] 完成今日自测

## 今日概念

> 随学习进度追加。

## 关键文件与调用链

- `mcp_server/src/main.cpp`
- `mcp_server/src/server/Server.h`
- `mcp_server/src/server/Server.cpp`
- `mcp_server/src/interface/ITransport.h`

当前入口：

```text
main()
  → 创建 PluginsLoader 与 Server
  → 选择 Transport
  → 加载 Plugin
  → OverrideCallback 注册业务回调
  → Server::Connect(transport)
```

## 实验结果

- 今日暂未执行构建或运行实验。
- 已知旧 `build/` 产物与当前 WSL glibc 不兼容；如需验证，将使用新的构建目录，不清理现有 `build/`。

## 疑问与临时记录

- 暂无。

## 待学习

- [ ] 构造函数为什么先注册默认方法
- [ ] `Connect()` 为什么是阻塞调用
- [ ] `ConnectAsync()` 与 `Connect()` 的差别
- [ ] `Stop()` 的资源释放顺序
- [ ] 析构函数再次调用 `Stop()` 是否安全

## 自测题

> 完成每节学习后追加答案和纠正点。

## 当日笔记整理状态

- 状态：已整理
- 正式笔记：`../笔记/第二天-协议与服务端核心.md`
- 说明：与 2026-08-11 的续学内容合并整理。
